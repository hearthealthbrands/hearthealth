{
  "name": "hearthealthbrands",
  "version": "1.0.0",
"scripts": {
  "start": "npx concurrently \"npx @11ty/eleventy --serve\" \"npx tailwindcss -i ./src/style.css -o ./_site/style.css --watch\"",
  "build": "npx @11ty/eleventy && npx tailwindcss -i ./src/style.css -o ./_site/style.css --minify"
},
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "concurrently": "^9.2.1"
  }
}
